run the commands one by one and resolve the errors. in order for the task to be successful, you need to be able to run all the commands without any errors or warnings:
- pnpm lint
- pnpm typecheck
- pnpm build
- pnpm dev